# Override only the recipient — everything else (course, signatory, branding,
# auto-date, auto-id) is inherited from config.ini.
#
# Run: python generate_certificate.py --config config.ini examples/certificate_recipient_override.ini

[certificate]
recipient_name = Jane Q. Attendee
recipient_email = jane@example.com
